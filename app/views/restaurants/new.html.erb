<% if @restaurants.errors.any? %>
  <section id="errors" >
    <h2> <%= pluralize(@restaurants.errors.count, "error") %> prohibited this restaurant from being saved: </h2>
    <ul>
      <% @restaurants.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </section>
<% end %>

<%= form_for @restaurants do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.submit %>
<% end %>
